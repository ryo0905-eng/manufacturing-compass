# AI外観検査 v2：追加学習量をそろえた4構成比較

固定日: 2026-09-22。ユーザーの「tugi」に基づく次段階。基準3モデルの安定化通過を受け、残る9モデルへ同じ追加学習を適用する。

- dirt-biased / normal-poor / label-errors、シード17・29・43の既存10エポック重みを使用。各構成の学習2,400枚で6エポック・450更新を追加。Adam 0.0003、バッチ32、同じ損失・モデル構造。Adamと乱数を各シードで再初期化。全16エポック・1,200更新。基準と同じ学習関数を使用し、構成別の早期停止や条件変更は行わない。
- 基準3本は既存の安定化モデル・評価をハッシュ検査して再利用。良否・領域の改善を目的に偏りモデルの設定を変更しない。追加前後の調整400枚も記録するが、偏りモデルが基準の性能条件を満たすことは評価実行の条件にしない。
- 全12条件でスコア0.5・最小面積12。同じ開発600枚（不良300・良品300、150基板×4画像）で評価する。確認済みの開発群であり、未知データ評価とは呼ばない。最終600枚は推論しない。
- 汚れ偏重・誤ラベルは傷の見逃しとIoU、良品模様不足は過検出の基準との差を主に確認。悪化・不変・改善を全て残し、悪化するまで学習やしきい値を調整しない。方向が3シードで一致した項目だけを、この設定で反復した傾向と呼ぶ。
- 各モデルの先頭24枚でPyTorch/ONNXのスコア最大差1e-4未満・後処理領域完全一致を確認。モデルハッシュ、親重み、学習回数、条件、全判定差分IDを記録する。表示候補は引き続き事前指定の17。
- 各コマンド175秒上限、上書き・別設定での再試行はしない。保存先 `.cache/ai-visual-inspection/v2/matched/<recipe>/`。旧10エポック比較と安定化記録は保持する。
- 今回は公平な学習構成比較の更新まで。照明・ルール比較・背景交換・しきい値曲線・最終評価・ブラウザ・UI・公開は次段階。
