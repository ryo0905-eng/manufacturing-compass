# AI外観検査 v2：追加学習の固定条件

設定日: 2026-09-21。ユーザーの「もう少し合格まで」の追加指示に基づく新しい実験。旧v2の320更新での未達記録と合格基準は変更しない。

1. 旧tinyモデルの重みから、同じ48枚・バッチ12・同じ損失で最大800更新を追加。Adamの状態は旧版で保存していないため再初期化し、学習率を0.0003に固定する。シード17、80更新ごとに確認する。旧320更新と合算した更新数も記録する。汚れ・傷IoU各0.70以上、各見逃し率5%以下、良品過検出率5%以下を2回連続で満たした時点で終了する。175秒上限。
2. 追加学習が通過した場合に限り、2,400枚の基準モデルをランダム初期化から学習する。tinyの重みは使わない。モデル・損失・しきい値0.5・最小面積12はv2と同じ。Adam 0.001、バッチ32、最大12エポック、シード17。調整用400枚で汚れ・傷IoU各0.50以上、各見逃し率20%以下、良品過検出率10%以下を2回連続で満たせば早期終了。175秒上限。
3. 基準モデルが通過した場合、設定・重みを固定して開発600枚を一度評価する。判定基準は調整群と同じ。結果を見てしきい値・重みを変更しない。Python/ONNXの変換誤差、良否・領域の一致も別に確認する。
4. いずれかが未達なら、その実験を完了し、同一ターン内で別設定にして再試行しない。最終評価600枚・ブラウザ実装・公開はこの追加学習の合格とは別段階とする。

保存先は `.cache/ai-visual-inspection/v2/followup/`。旧モデル・旧報告を上書きしない。学習画像、調整画像、開発画像の成績を別々に報告し、同じ指標のように比較しない。追加学習の成功は、未知画像への成功を意味しない。
