# AI外観検査ラボ

最終更新日: 2026-09-22

現状: **背景交換と固定した最終600枚の評価まで完了し、オフラインの事前主要条件を通過。Worker推論基盤は実装済み、Node上の実WASM数値照合は通過。比較UIは実装済み、実ブラウザの一周・スマホ幅は確認、スライダー修正後の追加確認は未完了**。[最終評価報告](./ai-visual-inspection-v2-holdout-validation.md)を参照。基準3シードは通常条件で見逃し0／300、過検出8・10・2／300、傷IoU 0.812・0.643・0.641。単純な汚れは基本ルールで見逃し0／50・過検出0／100。モデル・しきい値は開封前に固定し、全12モデルの変換照合も通過した。出力の微小な端点誤差だけを補正し、開発24枚×4モデル×3倍率でPythonとの領域完全一致を確認した。[比較UI](./ai-visual-inspection-ui.md)は5段階の試用版として実装した。[ブラウザ一周](./ai-visual-inspection-browser-validation.md)を通過し、追加確認で見つかったスライダーのラベルを修正。次はキーボード・連続操作・再試行を確認する。[端末内推論の実装記録](./ai-visual-inspection-web-runtime.md)を参照。実工場の性能保証・公開承認ではない。

最終群は2026-09-22に開封済み。この結果を使ってモデル・生成器・検査設定を開発変更する場合は、同群を開発用へ移し、新たな独立評価群を用意する。以下の履歴の「未使用」「未実施」は各実験当時の状態を示す。

初回v1は技術ゲート未達。[実測報告](./ai-visual-inspection-validation.md)と[全結果](./ai-visual-inspection-experiment.json)を参照。4構成×3シードの学習・開発評価は完了したが、偏り・誤ラベルの効果の再現性と、撮影条件の良否からの独立性が未達だった。以下の「独立に割り当てる」は要件であり、生成器v1の達成事項ではない。

追加検証v2: ユーザーの再検証指示により撮影条件と比較対象を修正。[v2プロトコル](./ai-visual-inspection-v2-protocol.md)を実行前に固定し、[48枚での学習確認](./ai-visual-inspection-v2-validation.md)を行った。データ監査は通過したが、少数画像での通過条件に未達のため本学習・UIへは進めていない。以下のv1の実験設定・証跡を遡って合格扱いにしない。v2は模様不足で変更する対象を良品画像だけに限定し、損失と段階判定を変更する。

その後の追加指示で、旧結果を保持したまま[追加学習の条件](./ai-visual-inspection-v2-followup-protocol.md)を固定。48枚での学習確認が追加240更新で通過し、新規初期化した2,400枚の基準モデルは調整群で10エポック目に通過。重み・しきい値を固定した開発600枚でも見逃し4／300・過検出17／300、汚れIoU 0.909・傷IoU 0.626で通過した。最終600枚には未着手。

次の指示で10エポック・750更新を全構成に固定して比較。汚れ偏重と誤ラベルで傷の見逃しが増え、良品模様不足で良品の過検出が増える傾向は、それぞれ3シードで再現した。一方、良品模様不足のシード43では傷の領域一致・見逃しが改善しており、基準モデルが全指標で勝つ説明にはしない。今回の学習11本（基準17は再利用）・評価12条件、ONNX照合と検出領域の画像確認を完了。今後は基準モデルの領域精度を調整群で安定させる条件を先に固定し、照明・ルール比較・最終評価へ進む。今回の開発結果を見てしきい値を変更していない。

2026-09-22の追加指示で、基準3本に学習率0.0003・6エポックを一律追加（Adamと乱数は再初期化）。しきい値・面積・合格基準は据え置き。3本とも調整400枚で通過した後、重みを固定して開発600枚を評価し、全て通過した。傷IoUは17: 0.789、29: 0.643、43: 0.638。見逃し0・2・0／300、過検出8・6・2／300。先頭24枚のPyTorch/ONNX領域も各モデルで一致。これは確認済みの開発群での結果であり、最終600枚は未使用。新16エポック基準と旧10エポック偏りモデルを混在させない。

2026-09-22の次段階で残る9本にも同じ6エポック追加を適用し、全12本を16エポック・1,200更新へ統一。基準3本の成果物はハッシュ照合して再利用した。シード17の見逃し／過検出は基準0／8、汚れ偏重134／0、良品模様不足0／9、誤ラベル8／0（母数は各300枚）。良品模様不足の過検出増加は全シードで1〜14枚と幅があり、必ず大幅に悪化する説明にしない。誤ラベルによる見逃しも8〜114枚と幅がある。全12本のONNX照合24枚、検出領域と集計の一致を確認し、比較画像を確認した。教材候補は引き続き事前指定のシード17とする。

2026-09-22のルール・明るさ検証では、調整400枚で基本しきい値112、補正しきい値8を固定。単純な汚れの基本ルールは見逃し0／50・過検出0／100で通過。開発600枚の倍率1.0では基本ルール118／8、補正ルール0／100、AI基準17は0／8（見逃し／過検出、母数は各300）。倍率0.7ではそれぞれ35／127、0／100、3／76。倍率1.3では152／0、6／100、19／54。全12モデルで実画素変換・再推論を行い、倍率1.0のみ既存スコアを検証して再利用した。しきい値曲線と実結果によるOR併用も記録。これは明るさ倍率だけの検証であり、撮像条件全体への保証にはしない。

2026-09-22の背景点検では、開発群内の背景だけを2通りに交換し、元背景での600枚の再描画一致を確認。基準3シードは両置換で従来基準を通過。モデル・データ・処理コード・設定をハッシュで固定してから最終群を開封し、ルール2方式と全12モデルを倍率0.7・1.0・1.3で評価した。最終通常条件でも汚れ偏重・誤ラベルによる傷見逃し増加、良品模様不足による過検出増加を各3シードで確認。改善・不変の指標も残す。背景交換は任意の背景への頑健性の証明とは扱わない。

## 目的と実装ゲート

架空の成膜後基板表面について、同じ画像に対する限定したルール処理と小型セグメンテーションモデルを比較する。合成画像は実際の光学系・製品規格を再現しない。約5分の日本語教材とし、方式全体の優劣、実工場での性能を保証しない。

最初にデータ生成、学習、調整、開発評価を実行する。単純な汚れにルールが有効で、データ構成やしきい値による複数画像の判定・領域の変化が3学習シードで確認できた場合のみUIへ進む。満たさない、または実行できない場合は未達を報告し、公開画面・導線は追加しない。最終評価は設定を固定した後だけ使用し、その後の変更に使った場合は開発評価へ移す。

## 検証仕様

- 128×128画素のグレースケール。良品は平坦・濃淡むら・規則模様、不良は汚れ・傷。元基板ごとに良品2枚、汚れ1枚、傷1枚を生成する。模様・照明・位置は良否から独立。
- 学習2,400枚、調整400枚、開発評価600枚、最終評価600枚。元基板・背景・欠陥形状テンプレート・変換派生は同一分割にまとめる。画像ハッシュ、生成由来、縮小画像の近似一致を検査する。単純な無地面の近似は別途報告し、未知の材質への一般化とは呼ばない。
- 学習構成は balanced / dirt-biased / normal-poor / label-errors の4種類。各2,400枚、同じ構造・初期化シード・学習手順。シードは17、29、43。構成間の画像重複は比較のため意図的に許容し、学習と評価の重複は禁止する。
- dirt-biasedは傷を汚れに置換。normal-poorは学習画像全体で規則模様を除き、背景と良否の交絡を避ける。label-errorsは傷の半数の学習マスクを空にする。評価の正解は変更しない。
- AIは16/32/64チャネルの2段U-Net、入力0〜1、出力sigmoidの欠陥スコア。事前学習済み重みの外部流用なし。推論入力にラベル・生成パラメータを含めない。
- ルールは暗い画素の二値化、8近傍連結成分、最小面積。補正版は局所平均との差を用いる。AIも同じ連結成分・面積基準から良否を導出。
- 調整群のみで見逃し率＋過検出率の最小値を選ぶ。同値では過検出の少ない点、設定順とする。この重み付けは調整用であり現場の採否を決めない。評価ではしきい値別の両指標も残す。
- 明るさ倍率0.7 / 1.0 / 1.3は評価前に固定。0〜255に丸め・飽和し、実画素を再推論する。正解マスクは維持する。
- 欠陥領域IoU、見逃し・過検出と母数、欠陥・模様別集計、背景差し替え比較、モデル容量、処理時間を保存する。確率校正済みとは表現しない。
- 1コマンド180秒未満。時間超過した検証は停止・未完了として報告し、設定を変えて再試行しない。

## ゲート通過後の画面・統合

公開プライバシーポリシーへの追記は、実際のブラウザ実装と同時に行う。技術検証だけの段階では未提供の機能を公開済みとして記載しない。開発時の合成画像・重み・判定はローカルに保持し、外部AIへ送信しない。公開後の方針は、画像・判定・学習状態の非送信、操作状態のメモリ保持、固定カテゴリ計測に限定する。

`/tools/ai-visual-inspection`。Server Componentの短い概要・使い方・限界・出典・更新日と、Client Componentの5段階操作を分離する。汚れ→模様と傷→学習例の異なるモデル→明るさ→別画像で確認と振り返り。スマホ上下・PC左右の同一画像比較、実出力の領域重ね、誤判定の絞り込み・拡大、キーボード・読み上げ対応。

常時表示は「不良品を通した数／不良品数」「良品をはじいた数／良品数」。詳細に混同行列と欠陥別結果。AIは4モデルの切替で、その場で学習したとは表示しない。代表例と実学習枚数を示す。最終確認では設定を固定し、再挑戦しても確認済み画像を初見扱いにしない。

処理はWorker内のONNX Runtime Web・WASM単一スレッド。画素・設定のみを推論へ渡し、真値は集計に分離。要求IDで古い結果を破棄。スコアをメモリに保持し、しきい値だけなら再推論しない。全画像の評価完了時に集計を一括更新し、失敗は未評価とする。モデル・WASMは同一サイトから遅延取得。外部API・アップロード・永続保存なし。

目標はモデル1MB以下、初回追加転送15MB以下（圧縮後）、プレビュー100ms以下、スマホ選択画像500ms以下、24画像3秒以下。未測定の目標を実績と表記しない。

併用は両方式の判定のORだけ。採用候補はルール／AI／併用／追加検証、採点なし。設定作業、データ収集、領域ラベルの準備負担を併記する。GRRは連続値の測定評価であり属性の良否判定と混同しない。

公開前に製造経験者3人の試用、画像・ラベル・文面の人間レビュー、依頼時のスマホ・キーボード・実端末性能確認を行う。未確認のまま公開可とはしない。canonical、WebApplication、パンくず、sitemapと既存ツール入口・検査解説への導線を用意する。検索需要は未検証。計測は固定カテゴリのみで画素・判定値・履歴を送信しない。

## 出典・利用条件

- [ONNX Runtime Web](https://onnxruntime.ai/docs/get-started/with-javascript/web.html)、[実行設定](https://onnxruntime.ai/docs/tutorials/web/env-flags-and-session-options.html)、[MIT License](https://github.com/microsoft/onnxruntime/blob/main/LICENSE)
- [PyTorch License](https://github.com/pytorch/pytorch/blob/main/LICENSE)
- [MVTec AD](https://www.mvtec.com/research-teaching/datasets/mvtec-ad)は非商用条件のため不使用。

出典確認日: 2026-09-21。自作生成画像・重みの利用条件と依存ライブラリの通知は配布前に整理する。外部画像・外部重みは使用しない。
